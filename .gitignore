# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
.venv/
venv/
ENV/

# Environment Variables
# .env is intentionally tracked — it holds only non-sensitive defaults.
# Put real local credentials in .env.local (never committed).
.env.local
.env.*.local
# Local secret snapshot downloaded for reference — never commit (contains real GOOGLE_CLIENT_ID + SMTP creds).
backend/env.download

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Docker
postgres_data/
docker-compose.override.yml

# OS Files
.DS_Store
Thumbs.db

# Claude Code harness session data
.claude/
frontend/.claude/
